/*
import { type Infer, type Type, type } from "arktype"

export type AssetInfo = {
    name: string
}

export const outputConfig = type({
    "assetFileNames?": "Function" as Infer<
        (assetInfo: AssetInfo) => (assetInfo: AssetInfo) => string
    >,
})
*/
