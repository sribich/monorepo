import { makeStyles } from "@sribich/fude"
import { create } from "@stylexjs/stylex"

export const unstyledListStyles = makeStyles({
    slots: create({
        container: {},
    }),
    conditions: {},
    variants: {},
    defaultVariants: {},
})
