import { colors } from "@sribich/fude-theme/vars/colors.stylex"
import { create } from "@stylexjs/stylex"

export const propsTableStyles = create({
    container: {
        backgroundColor: colors.background,
        color: colors.foreground,
    },
})
