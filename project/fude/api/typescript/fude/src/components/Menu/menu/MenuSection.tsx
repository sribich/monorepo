/* import { Separator } from "../separator/Separator"

export const MenuSection = () => {
    return <Separator />
} */

/*
ListItem.Section = function Section(
    props: PropsWithChildren<{
        title: string
        action?: ReactNode
        withDivider?: boolean
    }>,
) {
    const withDivider = props.withDivider ?? true

    return (
        <>
            {withDivider && <Divider className="my-2" />}
            <div role="button" className="w-full rounded px-1.5 py-0.5">
                <div className="flex">
                    <div className="flex items-center flex-1 cursor-default">
                        <Typography.Text className="text-sm" type="secondary">
                            {props.title}
                        </Typography.Text>
                    </div>
                    <div className="flex items-center flex-0 text-white/50">{props.action}</div>
                </div>
            </div>
        </>
    )
}
*/
