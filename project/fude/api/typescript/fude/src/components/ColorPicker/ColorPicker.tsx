///
///
///

export { Sketch } from "./variants/Sketch"

// "linear-gradient(to right, rgb(255, 0, 0),rgb(255, 255, 0),rgb(0, 255, 0),rgb(0, 255, 255),rgb(0, 0, 255),rgb(255, 0, 255),rgb(255, 0, 0))",

/* import { ElementRef,  useRef } from "react"

import { mergeProps, useMove, usePress } from "react-aria"

import { useComposedRefs } from "../../../hooks/useComposedRefs"
import { cn } from "../../../util/utils"
import { HsbColor } from "../color"
import { calculateHsbFromPosition } from "../util/shade"


export const Shade = <ShadeElement, ShadeProps>(({ color, setColor }, ref) => {

})

Shade.displayName = "Shade"
 */
