import { List } from "lucide-react"

import { ViewComponent } from "../components"
import { ListView } from "./ListView"

export const ListComponent: ViewComponent = {
    icon: List,
    component: ListView,
}
