[package]
name = "scheduler-infra"
version.workspace = true
authors.workspace = true
edition.workspace = true

publish = false

[features]
default = []

[dependencies]
shared = { path = "../shared" }
scheduler-app = { path = "../scheduler-app" }
scheduler-domain = { path = "../scheduler-domain" }

railgun = { workspace = true }

[dev-dependencies]

[lints]
workspace = true
