[package]
name = "storage"
version.workspace = true
authors.workspace = true
edition.workspace = true

publish = false

[features]
default = []

[dependencies]
shared = { path = "../shared" }
storage-app = { path = "../storage-app" }
storage-domain = { path = "../storage-domain" }
storage-infra = { path = "../storage-infra" }

railgun = { workspace = true }

[dev-dependencies]

[lints]
workspace = true
