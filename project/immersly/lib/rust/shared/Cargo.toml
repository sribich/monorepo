[package]
name = "shared"
version.workspace = true
authors.workspace = true
edition.workspace = true

publish = false

[features]
default = []

[dependencies]
shared-app = { path = "../shared-app" }
shared-domain = { path = "../shared-domain" }
shared-infra = { path = "../shared-infra" }

typegen = { workspace = true, features = ["uuid"] }

serde = { workspace = true }

[dev-dependencies]

[lints]
workspace = true
