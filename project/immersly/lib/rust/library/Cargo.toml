[package]
name = "library"
version.workspace = true
authors.workspace = true
edition.workspace = true

publish = false

[features]
default = []

[dependencies]
shared = { path = "../shared" }
library-app = { path = "../library-app" }
library-infra = { path = "../library-infra" }

railgun = { workspace = true }

[dev-dependencies]

[lints]
workspace = true
