[package]
name = "settings-app"
version.workspace = true
authors.workspace = true
edition.workspace = true

publish = false

[features]
default = []

[dependencies]
shared = { path = "../shared" }
settings-domain = { path = "../settings-domain" }

railgun = { workspace = true }

[dev-dependencies]

[lints]
workspace = true
