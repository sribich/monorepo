language: rust

tasks:

  coverage:
    command: cargo llvm-cov nextest --package=rpc
  test:
    command: noop
    deps:
      - coverage
      - mutation
  mutation:
    command: cargo mutants --test-tool=nextest
